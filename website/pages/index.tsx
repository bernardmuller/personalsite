import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";
import { Window } from "../components/Window";
import styles from "../styles/Home.module.css";
import Dock from "react-osx-dock";
import icon from "../public/instagram-Icon.png";
import bg from "../public/bg.png";

const Home: NextPage = () => {
	return (
		<div className={styles.container}>
			<Head>
				<title>Bernard Muller</title>
				<meta
					name="description"
					content="Generated by create next app"
				/>
				<link rel="icon" href="/favicon.ico" />
			</Head>

			<main className={styles.main}>
				<div
					style={{
						backgroundImage: `url(https://wallpaperaccess.com/full/752854.jpg)`,
						position: "fixed",
						width: "100%",
						height: "100%",
						backgroundRepeat: "no-repeat",
						backgroundSize: "100% 100%",
					}}
				></div>
				{/* <h1 className={styles.title} style={{ zIndex: "10" }}> */}
				{/* Welcome to <a href="https://nextjs.org">Bernard</a> */}
				{/* </h1> */}

				<Window />

				{/* <Dock
					width={500}
					magnification={0.6}
					magnifyDirection="up"
					debug
					style={{ position: "fixed", bottom: "0" }}
				>
					{["a", "b", "c", "d", "e"].map((item, index) => (
						<Dock.Item
							key={index}
							onClick={() => console.log(item)}
						>
							<Image src={icon} />
						</Dock.Item>
					))}
				</Dock> */}
			</main>
		</div>
	);
};

export default Home;
